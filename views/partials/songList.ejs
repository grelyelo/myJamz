<% songs.forEach(function(song) { %>
    <li data-id="<%= song._id %>" data-pos="<%= song.pos %>" class='songResult'><i class="fas fa-play-circle"></i><%= song.artist %> - <%= song.title %></li>
<% }) %>
